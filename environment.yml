name: warehouse-vision
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pytorch
  - torchvision
  - torchaudio
  - pytorch-cuda=11.8
  - pip
  - pip:
      - ultralytics
      - opencv-python
      - supervision
      - pandas
      - flask
      - requests
      - flask-cors